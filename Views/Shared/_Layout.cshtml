@{
    var currentPage = ViewData["Title"]?.ToString() ?? "";
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>@ViewData["Title"] - BabyMonitarr</title>
    <script type="importmap"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com"/>
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin/>
    <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@400;500;600;700&display=swap" rel="stylesheet"/>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"/>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css"/>
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true"/>
</head>
<body>
    <aside class="sidebar">
        <div class="sidebar-header">
            <i class="fas fa-baby sidebar-logo-icon"></i>
            <span class="sidebar-logo">BabyMonitarr</span>
        </div>
        <nav class="sidebar-nav">
            <a href="#" class="sidebar-link">
                <i class="fas fa-th-large"></i>
                <span>Dashboard</span>
            </a>
            <a href="#" class="sidebar-link">
                <i class="fas fa-wave-square"></i>
                <span>Audio Monitor</span>
            </a>
            <a asp-controller="Home" asp-action="Index" class="sidebar-link active">
                <i class="fas fa-sliders-h"></i>
                <span>Audio Settings</span>
            </a>
            <a href="#" class="sidebar-link">
                <i class="fas fa-video"></i>
                <span>Camera Settings</span>
            </a>
            <a href="#" class="sidebar-link">
                <i class="fas fa-cog"></i>
                <span>System</span>
            </a>
            <a href="#" class="sidebar-link">
                <i class="fas fa-info-circle"></i>
                <span>About</span>
            </a>
        </nav>
        <div class="sidebar-footer">
            <span>BabyMonitarr v1.0.0</span>
            <span>Made with <i class="fas fa-heart sidebar-heart"></i></span>
        </div>
    </aside>

    <div class="main-wrapper">
        <main class="main-content">
            @RenderBody()
        </main>
    </div>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
